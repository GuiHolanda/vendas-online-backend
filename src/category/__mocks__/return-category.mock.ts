import { ReturnCategory } from '../dtos/return-category.dto';
import { categoryMock } from './category.mock';

export const returnCategoryMock: ReturnCategory = {
  id: categoryMock.id,
  name: categoryMock.name,
};
